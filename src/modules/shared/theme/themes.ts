import { createTheme } from "@mui/material/styles";

export const appTheme = createTheme({
  palette: {
    primary: {
      main: "#083c84",
      light: "#13afe9",
    },
  },
  typography: {
    fontFamily: '"SanukRound", sans-serif',
  },
});
